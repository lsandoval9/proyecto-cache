#ifndef WORD_SIZE

#define WORD_SIZE 4 // Tamaño de la palabra en bytes - las palabras usualmente son de 32 bits o 64 bits

#endif

#ifndef ADDRESS_SIZE

#define ADDRESS_SIZE 32 // Tamaño de la direccion en bits

#endif

